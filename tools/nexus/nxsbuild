#!/usr/bin/env bash
# Wrapper for nxsbuild: runs the bundled AppImage binary with its Qt/GL libraries.
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export LD_LIBRARY_PATH="$here/runtime/lib:$LD_LIBRARY_PATH"
exec "$here/runtime/bin/nxsbuild" "$@"
